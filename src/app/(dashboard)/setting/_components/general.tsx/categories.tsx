import React from 'react';
// import { useGetCategories } from '@/lib/api/category/useCategory';

const Categories = () => {
  // const { data: categories, isPending } = useGetCategories();
  return <div className="space-y-2 py-2"></div>;
};

export default Categories;
